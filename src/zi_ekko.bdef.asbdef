managed implementation in class ZBP_I_EKKO unique;
strict ( 2 );

define behavior for ZI_EKKO alias Ekko
persistent table ekko
lock master
authorization master ( global )
{
  create;
  update;
  delete;

  validation validateMandatoryFields on save { create; update; }

  field ( readonly ) Ebeln;

  mapping for ekko corresponding
  {
    Ebeln = ebeln;
    Bukrs = bukrs;
    Lifnr = lifnr;
    Bedat = bedat;
  }
}

define behavior for ZC_EKKO alias Ekko
{
  use create;
  use update;
  use delete;
}